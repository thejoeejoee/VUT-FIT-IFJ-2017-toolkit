' Recursive function

function recursive(param as integer) as integer
	dim param0 as integer
	param0 = param - 1
	if param = 0 then
		return 42
	else
		return recursive(param0)
	end if
end function

scope
	dim a as integer
	a = recursive(4)
	print a;

end scope 